## Clang

### Dump memory layout

You can dump the memory layout of a `struct` or `class` with `clang`. See

<https://eli.thegreenplace.net/2012/12/17/dumping-a-c-objects-memory-layout-with-clang>


### `clang-format`

The full set of options is described
[here](https://clang.llvm.org/docs/ClangFormatStyleOptions.html).

 - Placement of `cv` qualifiers:

   ```yaml
   QualifierAlignment: Right
   ```
